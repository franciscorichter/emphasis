% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_ML.R
\name{pd_ML}
\alias{pd_ML}
\title{Parameter Discovery using Monte Carlo EM Algorithm}
\usage{
pd_ML(
  brts,
  lower_bound,
  upper_bound,
  verbose = TRUE,
  burn_in = 50,
  min_iterations = 100,
  tolerance = 0.001
)
}
\arguments{
\item{brts}{Numeric vector of branching times.}

\item{lower_bound}{Numeric vector specifying the lower bounds for each parameter.}

\item{upper_bound}{Numeric vector specifying the upper bounds for each parameter.}

\item{verbose}{Logical; if TRUE, detailed progress and diagnostics are printed.}
}
\value{
A list containing the final parameter estimates (`pars`) and a dataframe (`MCEM`)
        with iteration history including parameter estimates and their corresponding fhat values.
}
\description{
This function implements a Monte Carlo Expectation Maximization (MCEM) algorithm
to find suitable parameters for a model given branching times (brts) and parameter bounds.
}
\examples{
# Example usage:
brts_example <- runif(10, 0, 1)  # Example branching times
lower_bound_example <- c(0, 0, 0, 0)
upper_bound_example <- c(1, 1, 1, 1)
result <- pd_ML(brts_example, lower_bound_example, upper_bound_example, verbose = TRUE)

}
