% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gam.R
\name{estimate_likelihood_surface}
\alias{estimate_likelihood_surface}
\title{Estimate log-likelihood at a grid of parameter values via importance sampling}
\usage{
estimate_likelihood_surface(
  tree,
  pars_mat,
  model = "cr",
  sample_size = 200L,
  link = "linear",
  max_missing = 10000,
  max_lambda = 500,
  maxN = NULL,
  num_threads = 1L,
  bias_correct = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{tree}{A \code{phylo} object, branching-time vector, or anything
accepted by \code{\link{estimate_rates}}.}

\item{pars_mat}{Numeric matrix whose rows are compact parameter vectors
(same layout as \code{pars} in \code{\link{estimate_rates}}).}

\item{model}{Model specification: \code{"cr"}, \code{"dd"}, \code{"pd"},
\code{"ep"}, or a length-3 binary integer vector.}

\item{sample_size}{Number of augmented trees per grid point (default 200).}

\item{link}{\code{"linear"} or \code{"exponential"}.}

\item{max_missing}{Maximum missing lineages per augmentation (default 1e4).}

\item{max_lambda}{Maximum speciation rate (default 500).}

\item{maxN}{Total augmentation attempts per grid point.  If \code{NULL},
set automatically.}

\item{num_threads}{Number of threads for the C++ E-step.}

\item{bias_correct}{Logical; if \code{TRUE}, use the moment-based
bias-corrected IS estimator (truncated at order \code{K}).
Default \code{FALSE} uses the standard log-mean-exp estimator.}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
A data frame with columns for each parameter plus \code{fhat}
  (IS log-likelihood) and \code{n_trees} (number of valid trees obtained).
  Rows where IS failed have \code{fhat = NA}.
}
\description{
For each row of \code{pars_mat}, augments \code{sample_size} trees via IS
and computes the IS log-likelihood estimate (fhat).  Returns a data frame
ready for \code{\link{train_likelihood_GAM}}.
}
\seealso{
\code{\link{train_likelihood_GAM}}, \code{\link{find_MLE}}
}
\keyword{internal}
