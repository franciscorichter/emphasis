% Generated by hand
\name{estimate_rates_control}
\alias{estimate_rates_control}
\title{Default control parameters for estimate_rates}
\usage{
estimate_rates_control(method = c("mcem", "cem", "gam"), n_pars = 4)
}
\arguments{
\item{method}{Which method's defaults to return: \code{"mcem"},
\code{"cem"}, or \code{"gam"}.}

\item{n_pars}{Number of parameters (used to set the default \code{sd_vec}
for CEM when \code{NULL}).}
}
\value{
A named list of control parameters for the chosen method.
}
\description{
Returns a list of default tuning parameters for \code{\link{estimate_rates}}.
Override individual elements and pass the modified list via the
\code{control} argument.

Shared parameters (all methods): \code{lower_bound} (required, no
default), \code{upper_bound} (required, no default),
\code{verbose} (default FALSE), \code{max_missing} (default 1e4),
\code{num_threads} (default 1), \code{rho} (sampling fraction, a single
number in \eqn{(0, 1]}; default 1 = complete sampling, and an
out-of-range value is an error rather than being replaced by 1).

MCEM-specific: \code{sampling} (default \code{"bdi"}, the exact
birth-death-with-immigration sampler for the cr and dd models on the
linear and exponential links \emph{at \code{rho} = 1};
\code{"dynamic_fresh"} is the thinning sampler, used for every other
model/link, for \code{rho < 1} because the BDI proposal draws no
unsampled extant lineages, and whenever it is asked for.  A fallback is
announced with a message naming the reason and the sampler that runs),
\code{num_trees} (augmented trees per EM iteration, default 200;
alias: \code{sample_size}), \code{max_iter} (iteration cap, default 200),
\code{maxN} (max augmentation attempts per E-step, read by the thinning
sampler only and ignored by BDI; under \code{"dynamic_fresh"} it must be
at least \code{num_trees}; default NULL, and a non-finite value is treated
as NULL, = \code{max(2000, 10 * num_trees)}),
\code{xtol} (M-step optimiser tolerance, default 1e-3),
\code{tol} (parameter-stability threshold: the largest relative parameter
change \eqn{\max_j |\theta_k[j] - \theta_{k-1}[j]| / \max(|\theta_{k-1}[j]|, c)},
where the floor \eqn{c} is \code{tol / crown_age} for the rate-valued
links and \code{tol} for the log-rates of the exponential link, so that
the statistic does not change with the unit of time; the search box does
not enter; default 1e-2, so every coordinate must move by less than 1\% of
its own magnitude, \code{patience} times in a row; on a 12-tip CR tree at
\code{num_trees = 200} that statistic has median 0.02 and falls below 1e-2
in about a fifth of iterations, so runs at these defaults commonly end at
\code{max_iter}), \code{patience} (consecutive stable iterations
required, default 3).

CEM-specific: \code{max_iter} (default 20), \code{num_particles}
(particles per iteration, default 50; alias: \code{num_points}),
\code{num_trees} (trees per particle, default 1;
alias: \code{sample_size}), \code{sd_vec} (NULL = (upper-lower)/4),
\code{maxN} (default 10), \code{disc_prop} (default 0.5),
\code{tol} (plateau threshold, default 1e-4), \code{patience}
(default 5), \code{bias_correct} (default FALSE).

GAM-specific: \code{grid_points} (per-parameter resolution, default 20),
\code{sample_size} (IS trees per grid point, default 200),
\code{maxN} (NULL = auto), \code{max_lambda} (default 500),
\code{spline_type} (\code{"univariate"} or \code{"bivariate"}).
}
\keyword{internal}
