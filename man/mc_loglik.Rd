% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{mc_loglik}
\alias{mc_loglik}
\title{Monte Carlo log-likelihood via importance sampling with augmented trees}
\usage{
mc_loglik(
  brts,
  pars,
  sample_size,
  maxN,
  max_missing,
  max_lambda,
  lower_bound,
  upper_bound,
  xtol_rel,
  num_threads
)
}
\arguments{
\item{brts}{Numeric vector of branching times (crown age first,
sorted decreasing).}

\item{pars}{Numeric vector of model parameters.}

\item{sample_size}{Number of valid augmented trees to collect.}

\item{maxN}{Maximum total augmentation attempts (including failures).}

\item{max_missing}{Maximum number of extinct lineages per tree.}

\item{max_lambda}{Upper bound on the speciation rate (thinning bound).}

\item{lower_bound}{Lower bounds for parameter optimisation (same length
as \code{pars}).}

\item{upper_bound}{Upper bounds for parameter optimisation.}

\item{xtol_rel}{Relative tolerance for internal optimisation.}

\item{num_threads}{Number of threads for parallel augmentation.}
}
\value{
A named list:
\describe{
  \item{trees}{List of augmented-tree data frames (brts, n, t_ext, pd,
    id, parent_id).}
  \item{weights}{Numeric vector of log importance weights
    (\code{logf - logg}).}
  \item{fhat}{Monte Carlo log-likelihood estimate.}
  \item{logf}{Per-tree log-likelihood values.}
  \item{logg}{Per-tree log sampling probabilities.}
  \item{rejected}{Number of trees rejected (unhandled).}
  \item{rejected_overruns}{Rejected due to too many missing lineages.}
  \item{rejected_lambda}{Rejected due to lambda bound exceeded.}
  \item{rejected_zero_weights}{Rejected due to zero weight.}
  \item{time}{Elapsed time in milliseconds.}
}
}
\description{
Augments an observed tree (represented by its branching times) with
stochastically drawn extinct lineages, computes importance-sampling
weights, and returns a Monte Carlo estimate of the log-likelihood.
}
