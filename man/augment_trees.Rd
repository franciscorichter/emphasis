% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{augment_trees}
\alias{augment_trees}
\title{Draw augmented trees via importance sampling}
\usage{
augment_trees(
  brts,
  pars,
  sample_size,
  maxN,
  max_missing,
  max_lambda,
  num_threads,
  model = as.integer(c(0, 0, 0)),
  link = 0L,
  rho = 1
)
}
\arguments{
\item{brts}{Numeric vector of branching times (crown age first,
sorted decreasing).}

\item{pars}{Numeric vector of 8 model parameters
\code{c(beta_0, beta_N, beta_M, beta_D, gamma_0, gamma_N, gamma_M, gamma_D)}.}

\item{sample_size}{Number of valid augmented trees to collect.}

\item{maxN}{Maximum total augmentation attempts (including failures).}

\item{max_missing}{Maximum extinct lineages per augmented tree.}

\item{max_lambda}{Upper bound on the speciation rate (thinning bound).}

\item{num_threads}{Threads for parallel augmentation.}

\item{model}{Integer vector \code{c(use_N, use_P, use_E)}.}

\item{link}{Link function: \code{0} = linear, \code{1} = exponential.}
}
\value{
A named list:
\describe{
  \item{trees}{List of augmented-tree data frames.}
  \item{logf}{Per-tree log p(obs, z | theta).}
  \item{logg}{Per-tree log q(z | obs, theta).}
  \item{rejected}{Unhandled rejections.}
  \item{rejected_overruns}{Rejected: too many extinct lineages.}
  \item{rejected_lambda}{Rejected: lambda bound exceeded.}
  \item{rejected_zero_weights}{Rejected: zero IS weight.}
  \item{time}{Elapsed time (ms).}
}
}
\description{
Augments an observed extant tree (given by its branching times) with
stochastically drawn extinct lineages, and returns the simulated trees
together with their log proposal probabilities.  Scoring (\code{logf})
and IS aggregation (\code{fhat}) are intentionally left to the R layer
via \code{\link{eval_logf}} and \code{.is_fhat}.
}
\keyword{internal}
