% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gam.R
\name{train_likelihood_GAM}
\alias{train_likelihood_GAM}
\title{Fit a GAM to the IS log-likelihood surface}
\usage{
train_likelihood_GAM(
  surface,
  par_names = NULL,
  spline_type = c("univariate", "bivariate")
)
}
\arguments{
\item{surface}{Data frame returned by \code{\link{estimate_likelihood_surface}}.
Rows with \code{fhat = NA} are dropped automatically.}

\item{par_names}{Character vector of column names in \code{surface} to use
as predictors.  If \code{NULL} (default), all columns except \code{fhat}
and \code{n_trees} are used.}

\item{spline_type}{\code{"univariate"} (default) or \code{"bivariate"}.
See \code{\link{train_GAM}} for details.}
}
\value{
A \code{gam} object (Gaussian family) representing
  \eqn{\hat\ell(\theta) \approx \log L(\theta \mid \text{data})}.
}
\description{
Takes the output of \code{\link{estimate_likelihood_surface}} and fits a
smooth GAM approximation to the log-likelihood as a function of the model
parameters.
}
\seealso{
\code{\link{estimate_likelihood_surface}}, \code{\link{find_MLE}}
}
\keyword{internal}
